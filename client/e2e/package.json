{
  "name": "metawahl-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Visual regression baseline for the Metawahl CRA client (Phase 0).",
  "scripts": {
    "test": "playwright test",
    "test:update": "playwright test --update-snapshots",
    "record-fixtures": "node scripts/record-fixtures.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
